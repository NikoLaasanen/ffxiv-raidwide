{
  "rules": {
    "presence": {
      "$editLinkId": {
        ".read": true,
        ".write": true,
        "$sessionId": {
          ".validate": "newData.hasChildren(['sessionId', 'name', 'color', 'updatedAt'])"
        }
      }
    }
  }
}
